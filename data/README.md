# data/

- `uploads/` — images received via `POST /api/upload` (git-ignored, kept locally).
- `samples/` — put 2–3 sample drone/aerial images here for the demo.
  Large `.tif/.jpg/.png` samples are git-ignored; keep one small preview if needed.
