# Copy to .env to override the backend URL (optional).
# The Vite dev proxy already forwards /api/* to localhost:8000,
# so you only need this if the backend runs elsewhere.
VITE_API_URL=http://localhost:8000
