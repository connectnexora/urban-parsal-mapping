# models/

Place YOLO / segmentation weight files here in Step 2.

Planned:
- `yolov8n.pt` or a custom-trained building/road model (via `ultralytics`)
- Optional segmentation weights (e.g. YOLOv8-seg) for parcel/building boundaries

Large `*.pt / *.onnx / *.pth / *.bin` files are git-ignored by default —
download them separately and document the source + version here.
